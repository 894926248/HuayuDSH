# dsh-peak-valley

独立的峰谷插件。它在设置中提供「峰谷」分区，只显示：

- 工作日峰段：09:00–12:00、14:00–18:00（北京时间）
- 工作日其余时间及周末全天为谷段
- 当前时间所在时段
- 24 小时实时进度条

插件不显示电价、用量或费用合计，也不修改 `upstream/` 或产品前端注入层。

它通过 Web profile 的插件依赖独立加载；更新插件只需重启 Web Host，不触发 Electron 桌面程序打包。
