# Backend Product Layer

The backend is official-only. This directory records ownership and policy; it
must not contain a `source/` directory. Host, API, attachment, model, quota,
provider, persistence, and wire-contract changes belong upstream.
