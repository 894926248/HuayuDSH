# Frontend Product Layer

Browser-side product changes live under `source/` with their original upstream-relative paths. The overlay manifest records every file hash; the build injects these files into a disposable source worktree only.
